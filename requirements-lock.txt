# SUPERSEDED — DO NOT INSTALL FROM THIS FILE.
#
# Replaced in Phase 09 by:
#   requirements-rules-only.lock    the delivered rules_only runtime (CLI + UI)
#   requirements-dev.lock           rules-only runtime + pytest + mypy
#   requirements-local-model.lock   optional, rejected local-model runtime
#
# Retained for audit continuity only. It is intentionally comment-only so that
# installing from it cannot reintroduce the defects below. Historical
# references in docs/phase_reports/phase_01.md and
# submission/phase_reports/phase_01.md describe this file as it was at Phase 01
# and remain accurate for that point in history.
#
# ---------------------------------------------------------------------------
# WHY IT WAS UNSUITABLE
# ---------------------------------------------------------------------------
# 1. It installed the REJECTED local-model runtime. The snapshot pinned
#    llama_cpp_python==0.3.34 and diskcache==5.6.3, contradicting the delivered
#    rules_only runtime, the policy-3.3.1 model gate (enabled=false,
#    approval_status=rejected) and the zero-model-call release gate.
#
# 2. It OMITTED Streamlit and its entire transitive tree. streamlit==1.41.1 is
#    a declared runtime dependency in pyproject.toml, but neither it nor
#    altair, pandas, pyarrow, tornado, pydeck, protobuf, GitPython, requests,
#    pillow, blinker, cachetools, narwhals, tenacity, toml or watchdog appeared
#    in the snapshot.
#
# 3. Consequently it COULD NOT REPRODUCE the delivered rules-only application.
#    An environment built from it has no operator console, while still carrying
#    the rejected inference runtime — the exact inverse of the approved
#    delivery shape.
#
# The snapshot appears to predate the Phase 07 console work and was never
# refreshed. Note also that its packaging==26.2 and rich==15.0.0 pins differ
# from the versions resolved by pyproject.toml (packaging==24.2, rich==13.9.4),
# so it did not describe the environment that produced the accepted results.
#
# ---------------------------------------------------------------------------
# ORIGINAL CONTENTS (Phase 01 through Phase 08), preserved verbatim for audit
# ---------------------------------------------------------------------------
# annotated-types==0.7.0
# attrs==26.1.0
# click==8.1.7
# colorama==0.4.6
# diskcache==5.6.3
# et_xmlfile==2.0.0
# iniconfig==2.3.0
# jsonschema==4.23.0
# jsonschema-specifications==2025.9.1
# Jinja2==3.1.6
# llama_cpp_python==0.3.34
# markdown-it-py==4.2.0
# MarkupSafe==3.0.3
# mdurl==0.1.2
# mypy==1.11.2
# mypy_extensions==1.1.0
# numpy==2.5.1
# openpyxl==3.1.5
# packaging==26.2
# pluggy==1.6.0
# pydantic==2.9.2
# pydantic_core==2.23.4
# Pygments==2.20.0
# pytest==8.3.3
# referencing==0.35.1
# rich==15.0.0
# rpds-py==2026.6.3
# shellingham==1.5.4
# typer==0.12.5
# typing_extensions==4.16.0
