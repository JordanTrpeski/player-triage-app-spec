# SYNTHETIC holdout challenge set (Phase 03C)

**This data is entirely synthetic and was authored for independent challenge
testing. It is NOT copied or paraphrased from the supplied 40-row dataset.**
Expected results in `synthetic_holdout.json` were written **before** running the
engine and must not be altered after seeing system output without recording an
explicit adjudication event in the Phase 03C report.

Sensitive-looking values are synthetic test artefacts (e.g. the industry test PAN
`4111 1111 1111 1111`); no real player data is present. This directory is under
`tests/` and is not part of the shippable policy bundle.
