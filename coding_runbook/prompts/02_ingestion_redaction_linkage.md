# Phase 02 — Ingestion, Normalization, Linkage and Redaction

Prerequisite: Phase 01 passes.

Read:
- `policy/redaction_policy.json`
- `policy/linkage_policy.json`
- `policy/market_overlays.json`
- `input/dataset_player_messages.xlsx`
- `input/dataset_player_messages.csv`
- Ground-truth processing metadata for M09, M11, M18, M25, M29, M31 and M38.

Implement:
- Workbook/CSV ingestion with exact required columns and deterministic normalization.
- Input size/encoding validation.
- Local detectors for PAN with Luhn, CVV/authentication context, email, phone, player ID, transaction/ticket refs, currency amounts and prompt-injection patterns.
- Redaction in the configured order; never log matched values.
- Prompt-injection sanitization that preserves the legitimate M18 withdrawal request. If separation is uncertain, mark redaction uncertain and fail closed.
- Attachment fields: actual metadata is separate from text references. The supplied dataset has `attachment_received=false`; detect references without inventing a binary.
- Repeat-contact linkage using local player/reference metadata. M31 must link to M09. Raw player IDs/references must not enter operational outputs.
- Market overlay metadata from `market_overlays.json`.

Tests:
- M11 yields PAN/CVV flags, redacted output, and `bypass_sensitive`; no raw digits in logs/fixtures generated by tests.
- M18 yields prompt-injection flag and a sanitized legitimate withdrawal sentence.
- M31 links to M09 with first contact and previous-contact count.
- M29 and M38 mark references but not actual attachment receipt.
- Detector errors/uncertainty produce fail-closed status.

Do not implement semantic category classification yet.
Write `docs/phase_reports/phase_02.md` and stop.
